<VirtualHost *:80>
    ServerName sap-uat.dev
    Redirect permanent / https://sap-uat.dev/
</VirtualHost>

<VirtualHost *:443>
    ServerName sap-uat.dev

    DocumentRoot /var/www/html

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/sap-uat.dev.crt
    SSLCertificateKeyFile /etc/apache2/ssl/sap-uat.dev.key

    <Directory /var/www/html>
        AllowOverride All
        Require all granted
        Options -Indexes
    </Directory>

    # 🔒 Block hidden files (.env, .git, etc)
    <FilesMatch "^\.">
        Require all denied
    </FilesMatch>

    # 🔒 Block sensitive files
    <FilesMatch "\.(env|log|ini|sh|sql)$">
        Require all denied
    </FilesMatch>

    ErrorLog ${APACHE_LOG_DIR}/sap_uat_error.log
    CustomLog ${APACHE_LOG_DIR}/sap_uat_access.log combined
</VirtualHost>