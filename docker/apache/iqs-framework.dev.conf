<VirtualHost *:80>
    ServerName iqs-framework.dev
    Redirect permanent / https://iqs-framework.dev/
</VirtualHost>

<VirtualHost *:443>
    ServerName iqs-framework.dev

    DocumentRoot /var/www/html

    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/iqs-framework.dev.crt
    SSLCertificateKeyFile /etc/apache2/ssl/iqs-framework.dev.key

    <Directory /var/www/html>
        AllowOverride All
        Require all granted
        Options -Indexes
    </Directory>

    # 🔒 Block hidden files (.env, .git, etc)
    <FilesMatch "^\.">
        Require all denied
    </FilesMatch>

    # 🔒 Block sensitive files
    <FilesMatch "\.(env|log|ini|sh|sql)$">
        Require all denied
    </FilesMatch>

    ErrorLog ${APACHE_LOG_DIR}/iqs_framework_error.log
    CustomLog ${APACHE_LOG_DIR}/iqs_framework_access.log combined
</VirtualHost>